

<% if flash[:my_errors] %>
<% flash[:my_errors].each do |e|%>
<p><%= e %></p>

<% end %>
<% end %>



<%= form_for @mission do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :planet_id %>
<%= f.collection_select :planet_id, @planets, :id, :name%>

<%= f.label :scientist_id %>
<%= f.collection_select :scientist_id, @scientists, :id, :name %>



<%= f.submit %>
<% end %>
